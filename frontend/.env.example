# ChatVibe-AI 前端环境变量模板
# 用法：复制本文件为 .env 后填入真实值（.env 已 gitignore，不会提交）
#   cp .env.example .env

# ---------- AES-256-GCM 密钥（32 字节 base64）----------
# 必须与后端 application-local.yml 中的 chatvibe.security.aes.key 保持一致！
# 生成: openssl rand -base64 32
VITE_AES_KEY=CHANGE_ME_base64_32_byte_aes_key=
